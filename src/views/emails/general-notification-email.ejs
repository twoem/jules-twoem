<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= notificationTitle %></title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; margin: 0; padding: 0; background-color: #f4f4f4; }
        .container { max-width: 600px; margin: 20px auto; padding: 20px; background-color: #ffffff; border: 1px solid #ddd; border-radius: 5px; }
        .header { text-align: center; margin-bottom: 20px; padding-bottom: 15px; border-bottom: 1px solid #eee; }
        .header img { max-width: 150px; }
        .content { margin-bottom: 20px; }
        .content h1 { color: #0056b3; font-size: 22px; margin-bottom: 15px;}
        .message-body { font-size: 16px; line-height: 1.7; }
        .footer { text-align: center; font-size: 12px; color: #777; margin-top: 30px; padding-top: 15px; border-top: 1px solid #eee; }
        .button { display: inline-block; padding: 12px 25px; margin: 20px 0; background-color: #0056b3; color: #ffffff; text-decoration: none; border-radius: 5px; font-size: 16px; }
        a { color: #0056b3; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <% if (locals.logoUrl) { %>
                <img src="<%= logoUrl %>" alt="Twoem Online Productions Logo">
            <% } else { %>
                <h2>Twoem Online Productions</h2>
            <% } %>
        </div>
        <div class="content">
            <h1><%= notificationTitle %></h1>

            <% if (locals.studentName) { %>
                <p>Hello <%= studentName %>,</p>
            <% } %>

            <div class="message-body">
                <%- notificationMessage.replace(/\n/g, '<br>') %>
            </div>

            <% if (locals.actionLink && locals.actionText) { %>
                <p style="text-align: center;">
                    <a href="<%= actionLink %>" class="button"><%= actionText %></a>
                </p>
            <% } %>

            <p>If you have any questions, please don't hesitate to contact us.</p>
            <p>Sincerely,<br>The Twoem Online Productions Team</p>
        </div>
        <div class="footer">
            <p>&copy; <%= new Date().getFullYear() %> Twoem Online Productions. All rights reserved.</p>
            <% if (locals.siteUrl) { %>
                 <p><a href="<%= siteUrl %>">Visit our website</a> | <a href="<%= siteUrl %>/student/notifications">View all notifications</a></p>
            <% } %>
        </div>
    </div>
</body>
</html>
