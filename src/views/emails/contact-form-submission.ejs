<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Contact Form Submission</title>
    <style>
        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif, 'Apple Color Emoji', 'Segoe UI Emoji', 'Segoe UI Symbol'; line-height: 1.6; color: #333; margin: 0; padding: 0; background-color: #f0f8ff; /* Sky Blueish background */ }
        .email-wrapper { background-color: #f0f8ff; padding: 20px; }
        .container { max-width: 650px; margin: 0 auto; background-color: #ffffff; border: 1px solid #d4eaff; /* Lighter Sky Blue border */ border-radius: 8px; box-shadow: 0 4px 15px rgba(0,0,0,0.1); overflow: hidden; }
        .header { background-color: #007bff; /* Primary Sky Blue */ color: #ffffff; padding: 30px 25px; text-align: center; }
        .header img.logo { max-height: 70px; margin-bottom: 10px; }
        .header h1 { margin: 0; font-size: 26px; font-weight: 600; }
        .content { padding: 25px 30px; font-size: 16px; }
        .content p { margin-bottom: 15px; }
        .content strong { color: #0056b3; /* Darker Sky Blue */ }
        .message-section { background-color: #f9f9f9; border: 1px solid #eef5ff; /* Very Light Sky Blue */ padding: 20px; margin-top: 20px; border-radius: 5px; }
        .message-section h3 { margin-top: 0; font-size: 18px; color: #0056b3; margin-bottom: 10px; }
        .message-body { white-space: pre-wrap; /* Preserves line breaks from textarea */ }
        .footer { text-align: center; padding: 20px; font-size: 13px; color: #777; background-color: #eef5ff; border-top: 1px solid #d4eaff; }
        .footer a { color: #007bff; text-decoration: none; }
        .details-grid { display: grid; grid-template-columns: 120px 1fr; gap: 5px 10px; margin-bottom: 15px;}
        .details-grid dt { font-weight: bold; color: #555;}
        .details-grid dd { margin-left: 0; }
    </style>
</head>
<body>
    <div class="email-wrapper">
        <div class="container">
            <div class="header">
                <% if (locals.logoUrl) { %>
                    <img src="<%= logoUrl %>" alt="Twoem Online Productions Logo" class="logo">
                <% } %>
                <h1>New Contact Form Submission</h1>
            </div>
            <div class="content">
                <p>You have received a new message via the contact form on the Twoem Online Productions website.</p>

                <div class="details-grid">
                    <dt>From:</dt>
                    <dd><%= senderName %></dd>

                    <dt>Email:</dt>
                    <dd><a href="mailto:<%= senderEmail %>"><%= senderEmail %></a></dd>

                    <dt>Subject:</dt>
                    <dd><%= emailSubject %></dd>

                    <dt>Date:</dt>
                    <dd><%= submissionDate %></dd>
                </div>

                <div class="message-section">
                    <h3>Message:</h3>
                    <div class="message-body">
                        <%- emailMessage.replace(/\n/g, '<br>') %>
                    </div>
                </div>

                <p style="margin-top: 25px;">Please respond to this inquiry at your earliest convenience by replying directly to the sender's email: <a href="mailto:<%= senderEmail %>"><%= senderEmail %></a>.</p>
            </div>
            <div class="footer">
                <p>&copy; <%= new Date().getFullYear() %> Twoem Online Productions.</p>
                <% if (locals.siteUrl) { %>
                    <p><a href="<%= siteUrl %>">Visit Website</a></p>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>
